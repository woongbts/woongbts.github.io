name: Inspect recommendation data

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/tmp-inspect-recommendation-data.yml'

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inspect recommendation data
        run: python tmp_inspect_recommendation_data.py
      - name: Complete inspection
        run: echo done
