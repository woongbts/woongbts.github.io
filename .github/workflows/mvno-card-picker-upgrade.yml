name: MVNO card picker upgrade

on:
  push:
    branches: [main]
    paths:
      - 'scripts/mvno-card-picker-upgrade.py'
      - '.github/workflows/mvno-card-picker-upgrade.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply MVNO card picker upgrade
        run: python scripts/mvno-card-picker-upgrade.py
      - name: Commit updated calculator
        run: |
          if git diff --quiet -- rates.html assets/rates.js; then
            echo "No calculator changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add rates.html assets/rates.js
          git commit -m "Add MVNO card plan picker"
          git push
