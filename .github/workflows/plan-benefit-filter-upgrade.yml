name: Plan benefit filter upgrade

on:
  push:
    branches: [main]
    paths:
      - 'scripts/plan-benefit-filter-upgrade.py'
      - '.github/workflows/plan-benefit-filter-upgrade.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply plan benefit filter upgrade
        run: python scripts/plan-benefit-filter-upgrade.py
      - name: Commit updated calculator
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add rates.html assets/rates.js assets/rates.css
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Add benefit-focused mobile plan filters"
            git push origin main
          fi
